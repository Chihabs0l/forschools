# forschools

```javascript

function add(x , y) {
  return x + y
}

//lambda function

function lambda(x) {
  return function(y) {
    return x + y
  }
}

lambda(2)(4)

```

```cmd
@echo off

if %1 == -al (
  echo cmd
  echo srting;
)

```
