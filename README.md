# newscript

top start ther server!

```bash
npm run devStart
```
top build app!

```bash
npm run build
```

lambda function

```javascript
function lambda(x) {
    return function(y) {
        return x + y;
    }
}
```